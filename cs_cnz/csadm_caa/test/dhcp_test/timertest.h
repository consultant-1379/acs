
#ifndef THREADEX_H
#define THREADEX_H

static void * RunThread1(void *arg);		// Thread method
static void * RunThread2(void *arg);		// Thread method
static void * RunThread3(void *arg);		// Thread method
void safePrint(const char * str);

#endif
